// @flow

import { colors } from 'styles/variables'

const styles: {
  newMessage: {},
} = {
  newMessage: {
    flex: 1,
    backgroundColor: colors.grey2,
    height: 30,
    paddingLeft: 15,
  },
}

export default styles
