// @flow

import { colors } from 'styles/variables'

const styles: {
  newMessage: {},
} = {
  newMessage: {
    width: '100%',
    border: 0,
    backgroundColor: colors.grey2,
    height: '30px',
    paddingLeft: '15px',
    outline: 'none',
  },
}

export default styles
